import { Component, ChangeDetectionStrategy } from '@angular/core';

@Component({
  selector: 'app-judge-input',
  template: `<p>judge-input works!</p>`,
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class JudgeInputComponent {}
